apiVersion: v1
kind: Service
metadata:
  name: {{ include "dcms.fullname" . }}
  labels:
    {{- include "dcms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "dcms.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
